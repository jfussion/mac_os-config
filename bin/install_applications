#! /usr/bin/env bash

# DESCRIPTION
# Installs applications.

# EXECUTION

app_path="$HOME/apps"

printf "creating $app_path directory...\n"
mkdir -p $app_path

printf "Installing terminus using composer"

cd $app_path ; composer require pantheon-systems/terminus
